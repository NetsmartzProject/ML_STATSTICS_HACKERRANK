# 1️⃣ SELECT name → Retrieves only the name column from the table.
# 2️⃣ FROM Employee → Specifies that the data should come from the Employee table.
# 3️⃣ ORDER BY name → Sorts the retrieved names in ascending order (A to Z).


# SELECT name from Employee ORDER BY name;

# This is The Query for retrieving the names from the Employee table and sorting them in ascending order.